Realisation of algorithm searching poset X, which:
1) Lower cone of all elements of X is semilattice
2) Phi(X) is not transitive.

Usage:
$ swipl

?- [semi_lattice_research].
% semi_lattice_research compiled 0.00 sec, 19,856 bytes
true.

Find all 4-elements posets:
?- main(4).
true.

$> grep PASSED log_semi_lattice_phi.txt
$> 

There is no such 4-elements posets.

